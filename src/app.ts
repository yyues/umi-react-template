export function onRouteChange({ routes, matchedRoutes, location, action }) {
  if (matchedRoutes.length) {
    // document.title =
    //   process.env.APP_BASE_TITLE +
    //     '|' +
    //     matchedRoutes[matchedRoutes.length - 1].route.title || 'page';
  }
}
